title: Rafty
description: Rafting in Lori, Armenia
theme: jekyll-theme-cayman

# The custom index.html is served as-is (no front matter), so the theme
# only applies to any additional Markdown pages you add later.
